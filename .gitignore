# Rust build artifacts
/target/
**/*.rs.bk
Cargo.lock.bak

# IDE
.idea/
.vscode/
*.iml
*.swp
*.swo
.DS_Store

# Local environment
.env
.env.*
!.env.example

# Per-network deploy env: only the *.env.example templates are tracked.
# The real, host-local env files (DB creds, endpoints) are never committed;
# the treasury key is always provisioned out of band, never in these files.
ops/env/mainnet.env
ops/env/tn10.env

# Secrets - never commit any decrypted form
*.age
*.key
*.pem
!*.pub.pem
secrets.decrypted.*

# Coverage
/coverage/
*.profraw
tarpaulin-report.html
cobertura.xml
lcov.info

# Test artifacts
**/__snapshots__/
*.snap.new
/tests/fuzz/corpus/
/tests/fuzz/artifacts/
/pipeline-evidence/
/payout-evidence/
/api-evidence/

# SBOM / security artifacts (built on demand in CI)
/sbom/
*.cdx.json
*.spdx.json
*.sig
*.cert

# OS / tooling
node_modules/
.cache/
.direnv/

# Design audit
.design-audit/
cutover-evidence/
rust-toolchain.toml.orig
firecash-merged.yaml
bin/
*.log

# local dev: path override to firecash-rusty checkout (not for CI/others)
.cargo/

# Python bytecode
__pycache__/
*.pyc

# Redactor runtime state
redactor-payout-history.json

# Local operational snapshots (large zips / pre-change copies) — never committed
backups/
deploy-backups/
*.bak
*.bak.*

# Local operational snapshots (large zips / pre-change copies) — never committed
backups/
deploy-backups/
*.bak
*.bak.*

# Local incident notes and audits: they quote live miner IPs and per-worker
# behaviour, which are third-party data and must not be published.
pool27.txt
pool-problems-and-fixes.txt
docs/BRIDGE-3DAY-BUILD.md
docs/BRIDGE-BUILD-LOG.md
docs/BRIDGE-COMPLETION.md
docs/BRIDGE-FINAL-ARCH.md
docs/KASPA-MINER-COMPATIBILITY-AUDIT-2026-07-25.md
docs/KASPA-POOL-COMPATIBILITY-RESEARCH-2026-07-25.md
docs/POOL-EFFICIENCY-AUDIT-2026-07-25.md
docs/SOLO-POOL-MINIMAL-FIX-PLAN-2026-07-25.md
